cath_id: "1.10.490.10"
pdb_download_dir: "data/raw_pdbs"
processed_dataset_dir: "data/processed"
ssp:
  batch_size: 4
  learning_rate: 1e-3
  epochs: 100
segmentation:
  batch_size: 2
  learning_rate: 5e-4
  epochs: 200
  density_threshold: 0.5
